import React from 'react';
import PropTypes from 'prop-types';
import { Table, Popconfirm, Button } from 'antd';

const renameRole = React.createClass ({
  render(){
    return(
      <div>rename role</div>
    );
  }
});
export default renameRole;